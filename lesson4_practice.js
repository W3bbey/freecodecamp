
function findLongestWord(str) {
  var makeArray = str.split(" ");
  
  for (var i = 0; i < makeArray[i].length; i++){
     return makeArray[i].length;
  }
  
  return makeArray;
}

findLongestWord("The quick brown fox jumped over the lazy dog");
